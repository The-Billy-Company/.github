# Alignment Backlog

Small, real drift discovered while writing [`RELEASING.md`](../RELEASING.md)
that is genuinely inconsistent today, not yet owned by another effort, and
not worth silently declaring "standard" just because it's currently true
everywhere else. Nothing here blocks a release; each is a one-line fix
whenever a repository's workflows are next touched.

Nothing is open right now. The `actions/checkout` pin drift that used to be
listed here (`sheng` on a different commit than the rest of the org) was
resolved by converging `sheng` onto the shared pin
(`11d5960a326750d5838078e36cf38b85af677262`, tagged `v4`).

Add to this list rather than resolving drift silently in an unrelated PR —
the point is a paper trail, not a clean diff.
